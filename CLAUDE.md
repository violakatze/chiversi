# CLAUDE.md — 行政区域オセロゲーム

## プロジェクト概要

国土地理院の行政区域データ（GeoJSON）を使い、市区町村をオセロ盤面のマスに見立てたオセロゲーム。
GitHub Pages にデプロイするWebアプリケーション。

- 対戦形式: **人 vs CPU（AI対戦）**
- 対象地域: **千葉県**（将来的に他県対応の拡張性を持たせる）
- 地図表示: **実際のポリゴン地図（GeoJSON描画）**

---
## ゲームタイトル

Chiversi(チバーシ)

---
## 技術スタック

- **Vite + React + TypeScript**
- **OpenLayers** — 地図のポリゴン描画・インタラクション
- **MUI（@mui/material）** — UIコンポーネント
- デプロイ先: **GitHub Pages**（リポジトリ名: `chiversi`、`vite.config.ts` の `base: '/chiversi/'`）
- 隣接グラフ生成: **Python スクリプト（ビルド時事前計算）**
- GeoJSON簡略化: **mapshaper**（`npx mapshaper` で実行）

### ディレクトリ構成（推奨）

```
project-root/
├── CLAUDE.md
├── package.json
├── vite.config.ts
├── tsconfig.json
├── scripts/                    # ビルド時に実行するPythonスクリプト
│   ├── compute_adjacency.py    # GeoJSON → adjacency.json 生成
│   └── requirements.txt        # shapely, geopandas 等
├── data/
│   ├── 12.geojson              # 千葉県の行政区域データ（国土数値情報）
│   └── generated/
│       └── adjacency.json      # 事前計算された隣接グラフ（ビルド成果物）
├── public/
│   └── geojson/
│       └── chiba-simplified.geojson  # 表示用に簡略化されたGeoJSON
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types/                  # 型定義
│   ├── game/                   # ゲームロジック
│   │   ├── engine.ts           # 挟み判定・合法手計算
│   │   ├── direction.ts        # 方向ベース隣接走査（案2）
│   │   ├── initialPlacement.ts # 4環ベース初期配置
│   │   └── ai.ts               # CPU対戦ロジック
│   ├── graph/                  # 隣接グラフ関連
│   │   └── adjacency.ts        # adjacency.json の読み込み・型定義
│   ├── map/                    # OpenLayers地図表示
│   │   ├── MapView.tsx         # 地図コンポーネント
│   │   └── styles.ts           # ポリゴンのスタイル定義
│   └── components/             # UI部品
│       ├── GameBoard.tsx
│       ├── GameInfo.tsx
│       └── GameControls.tsx
└── .github/
    └── workflows/
        └── deploy.yml          # GitHub Actions: ビルド＆デプロイ
```

---

## ゲームルール

### 基本コンセプト

- 各市区町村 = オセロの1マス
- 石の色: 黒 / 白
- 1つの市区町村には1つの石のみ置ける
- 飛び地は本体と統合して1マスとして扱う（dissolve処理）

### 挟み判定: 案2「直線的な方向の概念を導入する」

通常のオセロは8方向に走査するが、不定形の行政区域では「方向」が自明ではない。
そこで**各市区町村の重心座標を使って方向を定義**する。

#### アルゴリズム

1. 各市区町村の重心（centroid）を事前計算しておく
2. 石を置いた市区町村 X の重心から、8方向（N, NE, E, SE, S, SW, W, NW）を定義する
   - 各方向は45度の扇形の範囲で判定
   - 例: 「東方向」= 重心から見て -22.5° 〜 +22.5° の範囲
3. ある方向について:
   - X の隣接市区町村のうち、その方向にある市区町村を1つ選ぶ（最も方向ベクトルに近いもの）
   - その市区町村が相手の色なら、さらにその市区町村の隣接からも同じ方向に辿る
   - 相手の色が連続した後に自分の色が見つかれば「挟んだ」と判定し、間の石を反転
4. 8方向すべてについてこれを行う

#### 方向判定の詳細

```typescript
// 重心A から重心B への角度を計算
function angle(a: [number, number], b: [number, number]): number {
  return Math.atan2(b[1] - a[1], b[0] - a[0]) * (180 / Math.PI);
}

// 8方向の定義（度数法、東=0°、反時計回り）
// ただし地図座標系では Y軸が緯度（北が正）なので注意
const DIRECTIONS = [
  { name: 'E',  center: 0 },
  { name: 'NE', center: 45 },
  { name: 'N',  center: 90 },
  { name: 'NW', center: 135 },
  { name: 'W',  center: 180 },  // or -180
  { name: 'SW', center: -135 },
  { name: 'S',  center: -90 },
  { name: 'SE', center: -45 },
];

// 隣接市区町村のうち、指定方向に最も近い1つを返す
// 方向範囲は ±22.5° の扇形
```

#### ポイント

- 方向走査は「隣接グラフの辺に沿って」行う。隣接していない市区町村への飛び越しはしない
- 同じ方向に隣接が複数ある場合は、方向ベクトルとの角度差が最小のものを選ぶ
- 1つの方向に候補がなければ、その方向の走査は行わない（=その方向では挟めない）

### 初期配置

1. 事前計算された隣接グラフから **4環（4-cycle）** を全列挙する
   - 4環 = A-B-C-D-A で、A~B, B~C, C~D, D~A がすべて隣接
2. 県の重心に近い4環を優先的に候補とする
3. 候補の中からランダムに1つ選び、黒白交互に配置する
   - A(黒) - B(白) - C(黒) - D(白)  のように対角が同色
4. 配置後に検証: 黒番・白番それぞれに少なくとも1つの合法手が存在するか
   - なければ再抽選
5. 4環が見つからない場合のフォールバック:
   - 隣接する2市区町村を選び、それぞれに黒白1つずつ配置
   - 合法手の存在を検証

### パスとゲーム終了

- 合法手がない場合はパス（相手の手番に移る）
- 両者ともパスの場合はゲーム終了
- 全マスが埋まった場合もゲーム終了
- 石の多い方が勝ち

---

## 隣接グラフの構築

### ビルドパイプライン

```
GeoJSON (国土数値情報)
  → scripts/compute_adjacency.py
  → data/generated/adjacency.json
  → React アプリが import
```

GitHub Actions で GeoJSON の push をトリガーに自動再生成する。

### adjacency.json の構造

```json
{
  "municipalities": [
    {
      "code": "12101",
      "name": "千葉市中央区",
      "centroid": [140.1234, 35.6078],
      "neighbors": ["千葉市稲毛区", "千葉市緑区", "千葉市美浜区", "千葉市若葉区", "市原市"],
      "neighborDetails": [
        { "name": "千葉市稲毛区", "borderLengthKm": 4.2, "type": "land" },
        ...
      ]
    },
    ...
  ],
  "cycles4": [
    ["佐倉市", "八街市", "四街道市", "千葉市若葉区"],
    ...
  ],
  "cycles4Central": [
    ["千葉市中央区", "千葉市緑区", "千葉市若葉区", "千葉市稲毛区"],
    ...
  ],
  "metadata": {
    "prefecture": "千葉県",
    "generatedAt": "2025-...",
    "sourceFile": "12.geojson",
    "municipalityCount": 59,
    "adjacencyCount": 133,
    "cycle4Count": 97,
    "cycle4CentralCount": 16
  }
}
```

- `cycles4Central`: 中心部（千葉市6区 + 四街道市・佐倉市・八千代市・習志野市・市原市・八街市・大網白里市）のみで構成される4環。初期配置でこちらを優先的に使用する。

### ステップ1: 陸地の境界共有による隣接判定

**共有頂点法**を使用する（intersection法と同精度で高速）。

```python
# アルゴリズム:
# 1. GeoJSON の coordinates を走査して全頂点を抽出
# 2. 各頂点を丸めて (round(lon, 7), round(lat, 7)) ハッシュマップに登録
# 3. 同じ頂点を持つ市区町村ペアをカウント
# 4. 共有頂点数 >= 2 のペアを隣接と判定（1だと点接触の可能性）
```

千葉県の実測値:
- 処理時間: 約700ms（Python）
- 市区町村数: 59（千葉市は区単位で分割: 12101-12106）
- 隣接ペア数: 133
- intersection法との差分: 0（完全一致）

**千葉市の区コード対応:**
| コード | 区名 |
|--------|------|
| 12101 | 千葉市中央区 |
| 12102 | 千葉市花見川区 |
| 12103 | 千葉市稲毛区 |
| 12104 | 千葉市若葉区 |
| 12105 | 千葉市緑区 |
| 12106 | 千葉市美浜区 |

### ステップ2: 連結性チェック

BFS/DFS でグラフの連結成分を列挙する。
千葉県の場合は全59市区町村が1つの連結成分に含まれるので追加処理は不要。

### ステップ3: 海域隣接の追加（ハイブリッド方式・案B）

他県対応を見据え、グラフが分断されている場合（島嶼部がある場合）に適用する。

```
1. 連結成分が1つだけ → 何もしない
2. 連結成分が2つ以上 →
   a. 各市区町村の重心を計算
   b. 孤立成分の各ノードについて、
      他の成分から最も近い k=3 個のノードを重心間距離で求める
   c. それらのペアを隣接グラフに追加（type: "sea" フラグ付き）
   d. 全体が連結になったことを再確認
```

- k=3（離島でも最低3つの隣接を確保し、挟み判定が成立するようにする）
- 陸地側の隣接関係は一切変更しない
- 追加された海域隣接は `type: "sea"` で区別し、地図上では破線表示する

### 飛び地の扱い

- 千葉県には30市区町村に飛び地が存在するが、大半は沿岸の小島嶼（面積比 0.000%）
- **ゲーム上は dissolve（統合）後の市区町村単位で扱う**
- 飛び地が本体とは異なる市区町村に隣接していても、それは本体の隣接先と同一であることが確認済み
- 地図表示では飛び地も本体と同色で塗る

---

## 地図表示（OpenLayers）

### GeoJSON の簡略化

- 元データ: 9.5 MB（`data/12.geojson`）
- 表示用に **mapshaper** で簡略化して 1MB 以下にする
  - コマンド例: `npx mapshaper data/12.geojson -simplify dp 10% -o public/geojson/chiba-simplified.geojson`
- 隣接判定には使わない（事前計算済みの adjacency.json を使う）
- `public/geojson/chiba-simplified.geojson` に配置し、fetch で読み込む

### ポリゴンのスタイリング

| 状態 | 塗りつぶし | 枠線 |
|------|-----------|------|
| 空きマス | 薄い灰色（半透明） | 細い灰色 |
| 黒の石 | 黒 or 濃いグレー | 黒 |
| 白の石 | 白 | 薄い灰色 |
| 着手可能マス（ハイライト） | 薄い緑（半透明） | 緑 |
| 最後に置かれたマス | 石の色 + 枠を太く | 黄色 or オレンジ |
| ホバー中のマス | 明るく | 枠を太く |

### 飛び地の表示

- 同じ市区町村の飛び地は本体と同色で塗る
- クリック時は本体と同じマスとして扱う
- 小さすぎる飛び地（面積比 0.01% 未満）は表示上フィルタしてもよい

---

## CPU（AI）対戦ロジック

### 難易度設計（段階的に実装）

1. **ランダム**: 合法手からランダムに1つ選ぶ（最初に実装）
2. **貪欲法**: 最も多く裏返せる手を選ぶ
3. **評価関数**: 隣接数が少ない端のマスを重視する（通常オセロの角に相当）
   - 隣接数1〜2の市区町村（例: 浦安市、館山市）は取られにくいため高評価

### 思考時間

- 人間の操作感を考慮し、CPUの着手は 500ms〜1000ms の遅延を入れる

---

## UI構成

```
+---------------------------------------------------+
|  Chiversi                                          |
+---------------------------------------------------+
|                                                     |
|              [OpenLayers 地図エリア]                 |
|              市区町村ポリゴン表示                    |
|              クリックで着手                          |
|                                                     |
+---------------------------------------------------+
|  手番: ● 黒（あなた） | スコア: 黒 12 - 白 8       |
|  [パス] [リスタート] [ルール説明]                    |
+---------------------------------------------------+
```

### 操作フロー

1. ゲーム開始 → 初期配置（4環ランダム）が地図上に表示される
2. 着手可能な市区町村がハイライトされる
3. プレイヤーがハイライトされた市区町村をクリック
4. 石が配置され、挟まれた石が反転するアニメーション
5. CPUが思考して着手（500ms〜1000ms後）
6. 3〜5を繰り返す
7. ゲーム終了時にモーダルでスコアと勝者を表示

---

## 千葉県データの実測値（参考）

ここでの調査結果を参考数値として記載する。

### 隣接関係

- 隣接ペア数: 133
- 最も隣接数が多い市区町村: 市原市（9隣接）
- 最も隣接数が少ない市区町村: 浦安市（1隣接）、館山市（1隣接）
- 短い境界の例: 千葉市若葉区↔東金市（0.4km）、八千代市↔白井市（0.8km）

### 4環（初期配置候補）

- 4環の総数: 97
- 全頂点が中心部の4環: 16件
  - 例: 千葉市中央区-千葉市緑区-千葉市若葉区-千葉市稲毛区
  - 例: 佐倉市-八街市-四街道市-千葉市若葉区
  - 例: 八千代市-千葉市花見川区-千葉市美浜区-習志野市
- 中心部の定義: 千葉市6区 + 四街道市・佐倉市・八千代市・習志野市・市原市・八街市・大網白里市

### 飛び地

- 飛び地を持つ市区町村: 30件
- 大半は沿岸の小島嶼（面積比 0.000%）で隣接に影響なし
- 千葉市中央区に約5km²の飛び地あり（面積比10%）
- ゲームの隣接グラフには影響しないことを確認済み

---

## 実装の優先順位

### Phase 1: MVP（最小限の動作するゲーム）

1. Vite + React + TypeScript プロジェクトセットアップ（MUI含む）
2. `adjacency.json` を `data/generated/` に配置、GeoJSONを簡略化して `public/geojson/` に配置
3. OpenLayers で千葉県の GeoJSON を表示
4. ゲームロジック（方向ベース挟み判定）を実装
5. 初期配置（`cycles4Central` からランダム選択、なければ `cycles4` にフォールバック）を実装
6. 人の着手（クリック → 反転）を実装
7. CPUの着手（ランダム、500ms〜1000msの遅延付き）を実装
8. パス・ゲーム終了判定
   - パス時: 画面にメッセージを表示（例:「合法手がありません。パスします」）
   - ゲーム終了時: 結果をモーダルで表示（勝者・スコア・リスタートボタン）
9. ルール説明ボタン（モーダルでルールを表示）
10. GitHub Pages にデプロイ

### Phase 2: 改善

1. CPUの難易度向上（貪欲法 → 評価関数）
2. 反転アニメーション
3. 着手履歴・undo
4. 小さすぎる飛び地のフィルタリング
5. レスポンシブ対応

### Phase 3: 拡張

1. 他県の GeoJSON 対応（海域隣接のハイブリッド方式を有効化）
2. ユーザーが GeoJSON をアップロードして遊べる機能
3. 人 vs 人（ローカル対戦）モード
4. 対戦結果の記録

---

## 注意事項・既知の課題

### ゲームバランス

- 浦安市（隣接1）や館山市（隣接1）は事実上「取られたら返せない」マスになりやすい
  → 通常のオセロの「角」に相当する戦略的価値を持つ
- 市原市（隣接9）のような多隣接マスは序盤に取ると不利（通常オセロの中央に相当）
- ゲームバランスはプレイテストで調整が必要

### 方向ベース判定の注意点

- 千葉県のように細長い形状の場合、南北に長いパスが生まれやすい
- 8方向の扇形が均等でない場合（隣接が偏っている場合）、特定の方向で挟めないことが多い
  → ゲーム性として許容する（通常のオセロでも端では方向が制限される）

### データのライセンス

- 国土数値情報の行政区域データは CC BY 4.0
- アプリ内に出典を明記すること: 「国土交通省国土政策局『国土数値情報（行政区域データ）』」
